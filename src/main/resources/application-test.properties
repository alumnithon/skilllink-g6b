spring.datasource.url=jdbc:mysql://${DB_HOST_TEST:localhost}:${DB_PORT_TEST:3306}/${DB_NAME_TEST:skilllink_db_test}
spring.datasource.username=${DB_USER_TEST:root}
spring.datasource.password=${DB_PASSWORD_TEST:passsword}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

api.security.secret=${JWT_SECRET:unsecure_jwt}
